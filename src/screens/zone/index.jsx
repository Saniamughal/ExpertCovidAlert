import React from "react";
import { View, StyleSheet, Text } from "react-native";

const Zone = () => {
  return (
    <View style={styles.container}>
      <Text>This is ZONE</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
});
export default Zone;
